import { ReactElement } from 'react';

export interface IconOptionsType {
  conversationIcon: ReactElement;
  secondIcon: ReactElement;
  ticketIcon: ReactElement;
  editIcon: ReactElement;
  closeIcon: ReactElement;
}
